## 修改目标
- 移除“管理账户”按钮
- 将“手动拉取”移到左侧列表上方，使用纯图标按钮（无文字）
- 移除左侧顶部的邮箱账号显示文本
- 隐藏页面内的顶部卡片（保留功能在下方卡片区域）
- 将 AI 工作台的打开方式改为：双击邮件列表某一项卡片即可打开
- 调整左侧文件夹列表宽度为更窄（从 12% 缩至约 9–10%）
- 移除右侧预览区标题处的“回复/回复全部/转发”按钮

## 具体变更点
- 文件：`client/pages/AIMarketing/EmailMarketingMailbox.tsx`
  - 顶部工具栏 Card：整体隐藏/移除（保留必要功能在下方卡片中）
  - 左侧列顶部控件区：
    - 保留“邮箱账户下拉切换”
    - 移除“管理账户”按钮
    - 在同一行加入“手动拉取”图标按钮（仅 `RefreshCw` 图标，不显示文字）
    - 去掉左侧列中 `activeAccount.email` 的展示文本
  - 列宽调整：
    - 左侧文件夹列由 `md:basis-[12%]` 改为更窄（例如 `md:basis-[10%]` 或固定宽度如 `md:w-[180px]`）
  - AI 工作台打开：
    - 移除“AI工作台”触发按钮
    - 在邮件列表项容器上添加 `onDoubleClick`，执行 `setAiDrawerOpen(true)` 并设置默认视图/标签（如 `review`）
  - 右侧预览区：移除标题区的“回复/回复全部/转发”按钮

## 验证与兼容
- 编译与类型检查确保无错误
- 点击列表单击仍选中项；双击打开 AI 工作台抽屉
- 手动拉取按钮显示为图标，点击后继续弹出“已手动拉取（模拟）”提示
- 布局：左列更窄，整体内容不溢出

## 备选与细节
- 若需进一步压缩左列宽度，可改为固定宽度并在移动端保持自适应
- 手动拉取图标可加 `title` 提示提升可用性（保持无文字）

## 实施顺序
1. 隐藏顶部卡片并移动必要控件到左列顶部
2. 移除管理账户按钮与邮箱文本，添加手动拉取图标按钮
3. 调整左列宽度
4. 改造 AI 工作台触发为双击列表项
5. 移除右侧预览区的回复相关按钮
6. 本地验证与修正细节