I will design a comprehensive prototype for the "Mapping & Recognition Layer" configuration page based on your requirements and the existing system context found in the codebase (referencing `EntityType`, `Incident`, and existing `EventRule` structures).

The output will be a detailed design document containing:

1.  **Information Architecture**: Positioning the page within the system.
2.  **Rule List Page Design**: Detailed column definitions and user actions.
3.  **Rule Editor Structure**: A modular breakdown (Modules A-F) covering:
    *   Basic Info & Event Source selection.
    *   Condition Logic (Field matching, RegEx, etc.).
    *   Business Anchor Mapping (linking events to unique Entity IDs).
    *   Instance & Relation Declarations.
    *   AI Trigger Configuration (MVP style).
4.  **Preview Functionality**: Design for a "Dry Run" feature to visualize rule outcomes.
5.  **MVP Constraints**: Explicit boundaries of the system capabilities.

I will present this as a structured text/Markdown response suitable for handing off to a UI/UX designer.