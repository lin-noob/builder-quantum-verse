# 企业数字业务建模系统设计文档

## 1. 核心理念与产品定位

### 1.1 从 CRUD 到 业务建模控制台
本系统不仅仅是一个数据的增删改查（CRUD）后台，而是一个**企业数字业务建模控制台 (Enterprise Digital Business Modeling Console)**。它面向业务负责人、架构师、风控专家及管理层，旨在构建企业数字业务的 Layer 2 —— **Digital Business Layer**。

### 1.2 核心原则：补充事实 vs 发明结构
- **补充事实 (Supplement Facts)**：用户不需要发明新的表结构或字段，而是基于系统提供的标准化语义框架，补充企业的业务事实（如：存在什么对象、能做什么操作、有什么底线规则）。
- **受控上下文 (Controlled Context)**：通过向导式、分步式的交互，引导用户在受控的上下文中完成建模，确保数据的完整性和业务合理性。
- **系统推导 (System Deduction)**：系统根据用户输入的事实，自动推导风险等级、责任归属建议、智能体（Agent）调用权限等，减少人工判断的偏差。

### 1.3 目标用户
- **业务负责人**：定义业务对象和行为能力。
- **企业架构师**：编排流程、定义生命周期和关联关系。
- **风控/合规专家**：设定业务底线规则和风险控制策略。
- **管理层**：查看模型完成度和风险概览。

---

## 2. 功能架构 (The 7 Pillars)

系统围绕七大核心模块构建，每个模块承载特定的业务语义：

### 2.1 业务对象 (Entities) - *What exists*
定义企业业务中存在的实体。
- **业务角色**：
  - **主数据 (Master)**：长期存在、被多方引用的核心对象（如：客户、商品）。
  - **交易对象 (Transaction)**：记录业务活动的对象（如：订单、支付单）。
  - **结果对象 (Result)**：业务过程产生的结论性对象（如：账单、报告）。
- **关键属性**：参与结算、影响风控、涉及合规、AI 可读/可写。

### 2.2 行为能力 (Capabilities) - *What can happen*
定义业务对象可以执行的操作或发生的行为。
- **场景驱动**：能力定义始于业务场景（如：“当客户下单时”、“当订单状态变为已支付时”）。
- **业务影响**：明确能力执行后对业务的影响（状态变化、金额变动、权益变更、仅记录通知）。
- **风险推导**：系统根据影响类型自动推导风险等级（High/Medium/Low）。

### 2.3 业务底线 (Rules) - *What must not break*
定义业务运行必须遵守的约束和规则。
- **规则类型**：
  - **禁止 (Blocking)**：违反规则时阻断操作。
  - **预警 (Warning)**：违反规则时允许通过但发出警报。
  - **审计 (Audit)**：违反规则时仅记录日志供审计。
- **附着机制**：规则不能独立存在，必须附着于对象、能力或生命周期迁移上。

### 2.4 生命周期 (Lifecycle) - *When it can happen*
定义业务对象的状态流转逻辑。
- **状态流转图**：使用图形化界面编辑状态（Nodes）和迁移（Edges）。
- **迁移控制**：定义从状态 A 到状态 B 允许触发的能力、是否需要审批、是否可回滚。

### 2.5 业务事件 (Events) - *What is worth noticing*
定义值得关注的业务变化，用于触发后续流程或通知。
- **订阅式创建**：事件通过订阅现有变化产生，禁止自由命名。
- **触发源**：
  - **状态变化**：如 Order Created -> Paid。
  - **规则触发**：如 大额支付预警触发。
  - **关键字段变化**：如 客户信用分变更。

### 2.6 责任与风险 (Responsibility) - *Who is accountable*
定义业务的责任归属和风险应对机制。
- **责任矩阵**：明确当某对象/能力发生特定情况时，由谁（业务负责人/风险负责人）负责。
- **升级策略**：定义问题未处理时的升级路径（时间阈值、升级对象）。

### 2.7 流程编排 (Processes) - *Allowed orchestration*
定义能力的允许执行顺序。
- **允许而非必须**：流程定义的是业务允许的路径，智能体（Agent）可在允许范围内自主决策，但不能越界。
- **节点类型**：能力执行、规则检查、状态判断。

---

## 3. 信息结构 (Information Schema)

### 3.1 Entity
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `name` | String | 业务对象名称 |
| `type` | Enum | Master / Transaction / Result |
| `business_tags` | Array | [结算, 风控, 合规, AI读写] |
| `description` | Text | 自然语言业务描述 (≥20字) |
| `relationships` | Array | 关联的其他对象及关系类型 |

### 3.2 Capability
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `name` | String | 能力名称 (系统建议或自定义) |
| `scenario` | Text | 业务场景描述 |
| `impacts` | Array | [状态变化, 金额变化, 权益变化, 记录] |
| `risk_level` | Enum | High / Medium / Low (系统推导) |
| `input_entities` | Array | 涉及的输入对象 |
| `output_entities` | Array | 产生的输出对象 |

### 3.3 Rule
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `name` | String | 规则名称 |
| `type` | Enum | 禁止 / 预警 / 审计 |
| `scope` | Enum | Entity / Capability / State |
| `violation_action` | Enum | 阻断 / 记录 / 上报 |
| `description` | Text | 规则逻辑的自然语言描述 |

### 3.4 Event
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `source_type` | Enum | StateChange / RuleTrigger / FieldChange |
| `condition` | Object | 具体的触发条件结构 |
| `derived_name` | String | 系统根据来源自动生成的事件名 |
| `subscribable` | Boolean | 是否可被外部订阅 |

### 3.5 Responsibility
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `target` | Ref | 关联的对象/能力/流程 |
| `business_owner` | Role | 业务负责人角色 |
| `risk_owner` | Role | 风险负责人角色 |
| `escalation` | Object | { condition, timeline, escalate_to } |

---

## 4. 交互设计 (UX Patterns)

### 4.1 页面式分步向导 (Page-based Wizard)
所有模块的新建和编辑操作均采用全页面、分步式向导设计，而非传统弹窗或抽屉。
- **步骤指示器**：顶部清晰展示当前步骤及完成进度。
- **分步内容**：
  - **基础信息**：业务语义描述。
  - **核心配置**：属性、状态、规则等核心定义。
  - **策略与治理**：风险、责任、权限等治理配置。
- **底部导航**：提供“上一步”、“下一步”、“保存草稿”、“创建并启用”等操作。

### 4.2 智能辅助与推导
- **风险提示**：在页面顶部常驻显示当前模型的高风险项（如未归属责任的高风险能力）。
- **自动推导**：根据用户选择的业务影响，自动计算风险等级；根据事件来源自动生成事件名称。
- **只读展示**：系统推导出的结果（如 Agent 权限、建议审批）以只读方式展示，强化“系统辅助决策”的感知。

### 4.3 图形化编排
- **生命周期**：仅通过画布编辑状态流转，直观呈现业务逻辑。
- **流程编排**：使用流程图定义能力的执行顺序，支持拖拽和连线。

---

## 5. 智能体与风控集成

### 5.1 智能体 (Agent) 交互
- **权限控制**：通过 Capabilities 的定义，明确 Agent 可调用的能力范围。
- **流程约束**：Processes 定义了 Agent 执行任务时的“围栏”，Agent 必须在流程定义的路径内行动。
- **可读写性**：Entities 的标签定义了 Agent 对数据的可见性和修改权限（需二次确认）。

### 5.2 风险控制
- **事前拦截**：Blocking 类型的规则直接阻断违规操作。
- **事中预警**：Warning 类型的规则触发实时通知。
- **事后审计**：Audit 类型的规则和所有操作记录均记入审计日志。
- **责任落实**：通过 Responsibility 模块，确保每个高风险操作都有明确的责任人和升级机制。
