# 消息中心功能使用指南

## 功能概述

消息中心是一个用于管理和查看系统消息的管理后台功能模块。它提供了以下主要功能：

1. **消息列表展示** - 显示所有系统消息，支持分页浏览
2. **消息筛选** - 按消息类型、状态和关键词进行筛选
3. **消息标记** - 将未读消息标记为已读
4. **消息删除** - 删除不需要的消息
5. **批量操作** - 支持批量标记已读和批量删除
6. **实时未读计数** - 在菜单项上显示未读消息数量

## 技术实现

### 后端服务 (messageCenterService.ts)

消息中心服务提供了以下API接口：

- `getMessages()` - 获取消息列表
- `markAsRead()` - 标记单条消息为已读
- `markMultipleAsRead()` - 批量标记消息为已读
- `deleteMessage()` - 删除单条消息
- `deleteMultipleMessages()` - 批量删除消息
- `getUnreadCount()` - 获取未读消息数量

### 前端组件 (MessageCenter.tsx)

消息中心页面组件包含以下功能模块：

- 消息表格展示
- 搜索和筛选功能
- 批量操作工具栏
- 分页组件
- 删除确认对话框

### 路由配置 (AdminApp.tsx)

在管理后台路由配置中已添加了消息中心的路由：

```jsx
<Route path="messages" element={<MessageCenter />} />
```

### 菜单配置 (AdminLayout.tsx)

在管理后台布局中已添加了消息中心菜单项，并实现了未读消息数量的实时显示。

## 如何添加菜单项

### 方法一：通过菜单管理界面（推荐）

1. 登录管理后台，使用具有菜单管理权限的账号
2. 在左侧导航栏中找到"系统管理" -> "菜单管理"
3. 点击"添加菜单"按钮
4. 填写以下信息：
   - **菜单名称**: 消息中心
   - **父级菜单**: 系统管理（或根据需要选择）
   - **菜单类型**: 菜单
   - **路由地址**: `/admin/messages`
   - **组件路径**: `/client/admin/pages/MessageCenter.tsx`
   - **菜单图标**: Bell（铃铛图标）
   - **菜单排序**: 7
   - **是否显示**: 是
   - **菜单状态**: 启用
5. 点击"确定"保存配置

### 方法二：直接修改代码

如果需要直接在代码中添加菜单项，可以修改 `AdminLayout.tsx` 文件中的菜单配置。

## 功能测试

### 单元测试

消息中心组件包含完整的单元测试，可以通过以下命令运行：

```bash
npm test client/admin/pages/MessageCenter.test.tsx
```

测试覆盖了以下场景：
- 组件正常渲染
- 空状态显示
- 错误处理

### 手动测试

1. 启动开发服务器：`npm run dev`
2. 访问管理后台：http://localhost:8080/admin
3. 登录具有相应权限的账号
4. 点击左侧菜单中的"消息中心"
5. 验证以下功能：
   - 消息列表正常显示
   - 搜索和筛选功能正常
   - 标记已读功能正常
   - 删除功能正常
   - 批量操作功能正常
   - 未读消息计数显示正常

## 常见问题

### 1. 消息中心页面无法访问

**问题原因**: 菜单配置不正确或路由未正确设置

**解决方案**:
- 检查菜单管理中的路由地址是否为 `/admin/messages`
- 确认 `AdminApp.tsx` 中是否包含消息中心路由配置
- 检查浏览器控制台是否有错误信息

### 2. 未读消息计数不显示

**问题原因**: 未读消息API调用失败或权限不足

**解决方案**:
- 检查网络请求，确认 `/api/admin/api/v1/messages/unread-count` 接口是否正常返回数据
- 确认当前账号是否具有访问消息中心的权限

### 3. 批量操作功能异常

**问题原因**: API接口问题或网络连接异常

**解决方案**:
- 检查网络请求，确认批量操作相关接口是否正常工作
- 查看浏览器控制台和服务器日志，定位具体错误

## 扩展开发

### 添加新的消息类型

1. 在 `messageCenterService.ts` 中的 `MessageType` 枚举中添加新的类型
2. 在 `MessageCenter.tsx` 中的 `messageTypeMap` 中添加对应的显示配置
3. 更新相关API接口以支持新的消息类型

### 自定义消息展示

可以根据需要修改 `MessageCenter.tsx` 组件，自定义消息的展示方式，例如：
- 添加消息详情弹窗
- 支持消息内容的富文本展示
- 添加消息分类标签

### 集成推送通知

可以集成 WebSocket 或其他实时通信技术，实现消息的实时推送功能。